import React from 'react';

const Hero: React.FC = () => (
    <section className="p-8 pt-8 pb-6 mb-8 flex-1 w-full">  
    <h1 className="m-0 mb-3 text-3xl font-bold text-[var(--text-secondary)]">A PLAN</h1>
    <p className="text-lg">
      to <strong className="text-[var(--text-secondary)]">grow</strong> our grassroots,<br />
      to <strong className="text-[var(--text-secondary)]">drive</strong> committed membership,<br />
      to <strong className="text-[var(--text-secondary)]">nurture</strong> new ideas,<br />
      to <strong className="text-[var(--text-secondary)]">win</strong> back the trust of Ontarians,<br />
      to <strong className="text-[var(--text-secondary)]">win.</strong>
    </p>
  </section>
);

export default Hero; 